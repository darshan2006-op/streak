# Streak

streak is a cross game engine.

# Build Intructions
streak uses cmake for building the project 

run the following command to build in debug mode:
```bash
cmake -S . -B build
```

currently, release configurations are not implemented.

